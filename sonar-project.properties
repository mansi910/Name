# Required metadata
sonar.projectKey=org.sonarqube:js-simple
sonar.projectName=ESLint_CheckStyle_sample
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=.

# Language
sonar.language=js

# Encoding of sources files
sonar.sourceEncoding=UTF-8
